// add solution here
function theBeatlesPlay(musicians, instruments) {
  var empty = [];
  for (var i=0; i < 4; i++) {
    empty.push(`${musicians[0]} plays ${instruments[0]}`);
  }
  return empty;
}

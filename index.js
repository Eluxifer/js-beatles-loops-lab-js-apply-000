// add solution here
function theBeatlesPlay(musicians, instruments) {
  var empty = [];
  for (var i=0; i < 2; i++) {
    empty.push(`${musicians[i]} plays ${instruments[0]}`);
  }
  return empty;
}
